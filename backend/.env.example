# =============================================================================
# ActivEducation Backend - Variables d'Environnement
# =============================================================================
# Copiez ce fichier vers .env et remplissez les valeurs
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# ENVIRONNEMENT
# -----------------------------------------------------------------------------
# Options: development, staging, production
ENVIRONMENT=development

# Mode debug (doit etre False en production)
DEBUG=True

# -----------------------------------------------------------------------------
# SUPABASE (OBLIGATOIRE)
# -----------------------------------------------------------------------------
# URL de votre projet Supabase
SUPABASE_URL=https://your-project.supabase.co

# Cle anonyme (anon key) - pour operations cote client
SUPABASE_KEY=your-anon-key-here

# Cle service role (optionnel) - pour operations admin
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# -----------------------------------------------------------------------------
# SECURITE JWT (OBLIGATOIRE)
# -----------------------------------------------------------------------------
# Cle secrete pour signer les tokens JWT
# IMPORTANT: Generez une cle unique avec: python -c "import secrets; print(secrets.token_urlsafe(48))"
# Minimum 32 caracteres requis
SECRET_KEY=your-super-secret-key-minimum-32-characters-long

# Algorithme de signature (ne pas modifier sauf si necessaire)
ALGORITHM=HS256

# Duree de validite du token d'acces en minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Duree de validite du refresh token en jours
REFRESH_TOKEN_EXPIRE_DAYS=7

# -----------------------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# -----------------------------------------------------------------------------
# Liste des origines autorisees, separees par des virgules
# Exemple: http://localhost:3000,http://localhost:8080,https://app.activeducation.com
# ATTENTION: Ne jamais utiliser "*" en production
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5173

# -----------------------------------------------------------------------------
# RATE LIMITING
# -----------------------------------------------------------------------------
# Nombre maximum de requetes par minute par IP
RATE_LIMIT_PER_MINUTE=60

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
# Niveau de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
